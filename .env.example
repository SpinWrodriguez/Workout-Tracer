# Supabase project the nutrition app already uses. Copy to .env.local and
# fill in from that app — the workout app reads the same account so the
# weigh-in history syncs without an export/import round trip.
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
